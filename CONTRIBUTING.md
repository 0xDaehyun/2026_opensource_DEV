# 기여 및 팀 개발 가이드

## 시작

```bash
./gradlew clean test
./gradlew :app:bootRun
```

두 명령 중 하나라도 실패하면 기능 개발 전에 팀장에게 실행 환경과 로그를 공유한다.

## 작업 선택

1. [`TODO.md`](TODO.md)에서 선행 조건이 끝난 미할당 항목을 선택한다.
2. 담당자 이름을 GitHub Issue 또는 프로젝트 보드에 기록한다.
3. TODO 번호로 브랜치를 만든다.

```text
feat/adapter-01-order-status
feat/scenario-02-validator
fix/core-03-cancel-invariant
```

## PR 규칙

- 하나의 PR은 하나의 TODO만 해결한다.
- 모듈 경계를 넘지 않는다. 통합 PR만 `app`과 여러 모듈을 연결할 수 있다.
- 정상 테스트와 실패 테스트를 하나 이상 포함한다.
- 요청·응답 또는 입력·출력 예시를 PR에 작성한다.
- 새 기능이 아닌 리팩터링을 함께 넣지 않는다.
- 다른 팀원 한 명의 승인을 받은 후 병합한다.

## 커밋 메시지

한국어 Conventional Commits를 사용한다.

```text
feat: 주문 상태 조회 handler 추가
test: 잘못된 체결 비율 검증 추가
fix: 부분체결 취소 시 미체결 금액만 반환
docs: 시나리오 작성법 보완
```

## 완료 기준

- 담당 모듈 테스트가 통과한다.
- `./gradlew test`가 통과한다.
- TODO 체크 항목이 모두 충족된다.
- 작성자가 코드 흐름을 설명할 수 있다.
- 주석으로 기능을 숨기거나 테스트를 비활성화한 채 완료 처리하지 않는다.

## 도움 요청

30분 이상 막히면 다음 내용을 정리해 팀에 공유한다.

```text
하려던 일:
기대한 결과:
실제 결과:
시도한 방법:
관련 테스트 또는 오류 로그:
```
