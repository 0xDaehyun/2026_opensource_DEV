package io.github.stockmock.scenario.execution;

/**
 * TODO(SCENARIO-04) 테스트 목록:
 * <ul>
 *   <li>100주 × ratio 0.3은 30주</li>
 *   <li>quantity는 그대로 체결 수량 사용</li>
 *   <li>ratio/quantity 동시 사용과 둘 다 누락을 거부</li>
 *   <li>전체 체결 수량이 주문 수량을 넘으면 거부</li>
 *   <li>같은 입력에서 동일 FillPlan 생성</li>
 * </ul>
 */
class ScenarioFillPlanProviderTest {
}
