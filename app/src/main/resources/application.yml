spring:
  application:
    name: stock-mock-server

server:
  port: 8080

mock:
  scenario: classpath:scenarios/basic/partial-fill.yml
  clock:
    origin: 2026-01-02T00:00:00Z
    mode: ATTACHED
  token:
    ttl: 24h
